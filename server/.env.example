PORT=5000

# OpenAI — get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Which GPT model to use
GPT_MODEL=gpt-5-nano

# Allowed CORS origin (comma-separated). Leave blank to default to localhost:3000.
# In production on Render this isn't needed since the client is served by the same
# Express process, but set it if you ever split the services.
# CORS_ORIGIN=https://your-app.onrender.com
